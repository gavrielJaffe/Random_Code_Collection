import subprocess

def job():
    subprocess.run(["python3", "extract_excel.py"])

# Run the job once
job()
print("from the run job.py in the autoscripts")
print("from the run job.py in the autoscripts")
print("from the run job.py in the autoscripts")
print("from the run job.py in the autoscripts")
# The script will now exit without any scheduling
